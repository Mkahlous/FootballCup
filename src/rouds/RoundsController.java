package rouds;

import teams.Teams;

import java.util.ArrayList;

public class RoundsController {

    public static void getWinningTeam(ArrayList<Teams> teams) {
        //ArrayList<Teams>allTeams = new ArrayList<>();
        //All members.
        ArrayList<Teams> firstGroup = new ArrayList<>();
        //firstGroup contain 4 random teams
        ArrayList<Teams> secondGroup = new ArrayList<>();
        //secondGroup contain 4 random teams
        for (int i = 0; i < teams.size(); i = i + 2) {

        }


    }
    public static void matchResult() {
        int team1Goals = (int) Math.random();
        int team2Goals = (int) Math.random();

        if (team1Goals > team2Goals) {

        }



    }
}
